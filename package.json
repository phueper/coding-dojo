{
  "name": "coding-dojo",
  "repository": "https://github.com/actano/coding-dojo",
  "version": "0.0.0",
  "description": "Coding Dojo",
  "scripts": {
    "test": "mocha -R spec --compilers coffee:coffee-script/register **/*-test.coffee",
    "coverage": "coffeeCoverage ./lib ./coverage && mocha -R spec --require coffee-coverage/register --reporter html-cov coverage/**/*-test.js > coverage/coverage.html && open coverage/coverage.html"
  },
  "author": "",
  "dependencies": {
    "coffee-coverage": "^0.4.4",
    "chai": "^1.9.1",
    "coffee-script": "^1.7.1",
    "mocha": "^1.20.1",
    "commander": "^2.6.0"
  }
}
